#General
create_log: 0
log_file_dir: "/tmp/"

#Mail Replica Params
#Domain of accounts to be promoted, if for all account leave it empty
domain_to_replicate: "example.com"
#The server where db connectors are runnin
pg_server: 
pg_port: 5432
#Get dollowing values running
# consul kv get -recurse -token-file="/etc/carbonio/advanced/service-discover/token" "carbonio-ha"
pg_user: "ha"
pg_password: '**************'
pg_db: "ha"
#The application server where to replicate accounts
dst_hostname: "replicaServer"
